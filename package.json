{
  "name": "eatsy",
  "version": "0.1.0",
  "description": "Eatsy - FnB web app integrating students with canteen tenants to minimize queues.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "lint": "eslint \"src/**/*.js\" \"public/**/*.js\"",
    "lint:fix": "eslint \"src/**/*.js\" \"public/**/*.js\" --fix",
    "smoke": "node -e \"const app = require('./src/app'); const s = app.listen(0, () => { console.log('Smoke OK on port', s.address().port); s.close(); });\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0"
  }
}
